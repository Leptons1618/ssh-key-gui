# User Taste Profile
- Plans major work in detail before implementation: asked to "plan the architecture in details" first for the Go/TUI/GUI rewrite, and approved a phased plan plus key fork questions (language, GUI stack, rollout) before any code was written. Confidence: 0.95
- Prefers low-risk migrations: chose to keep the Python version running alongside the Go rewrite and delete it only after a parity checklist passes (explicitly rejected immediate replacement). Confidence: 0.85
- When offered options, picks the recommended/safe choice (selected Go, native toolkit, and parallel rollout — all marked Recommended). Confidence: 0.6
- Rewrite targets **Go** (chosen over Rust): Bubble Tea + lipgloss + bubbles for the TUI, Fyne for the native GUI, one binary (`--gui` default, `--tui`, `--version`). Confidence: 0.9
- Architecture convention: all business logic lives in `internal/core` with zero UI imports; frontends shell out to OpenSSH tools — no in-process ssh-agent protocol, strict feature parity with UX copy preserved. Confidence: 0.8
- UI uses the "cold forge" palette: cool graphite neutrals, ink `#1b2025`, teal accent `#006d68` owns primary/active/verified states, danger `#9e2d28`, amber `#895c07` is the dedicated warning role (AGENT OFF renders amber = caution, not error). All color pairs meet WCAG AA (≥4.5 body, ≥3 disabled/UI). Tokens live in `internal/core/types.go`. Confidence: 0.9
- No passwordless sudo available: toolchains get installed userland-style under `~/.local` (Go at `~/.local/go`; GL/X11 headers in `~/.local/include`; linker symlinks + hand-written `gl.pc` in `~/.local/lib`). Building requires sourcing `env.sh` in the repo root (sets PATH, LIBRARY_PATH, PKG_CONFIG_PATH, CGO_CFLAGS). Confidence: 0.9
- Package manager is dnf on Fedora; when devel RPMs are needed, download + `rpm2cpio | cpio` extraction into `~/.local` works without sudo. Confidence: 0.7
- Tests must never touch the real `$HOME`: always sandbox it (e.g. `t.Setenv("HOME", t.TempDir())`, script-managed sandbox dirs). A test once overwrote the real `~/.ssh-key-gui-state.json` because a package-level path var froze at init — paths must resolve per call so HOME swaps take effect. Confidence: 0.85
- TUI verification pattern that works here: drive the binary through a real pty (child needs `setsid` + `TIOCSCTTY`), set `TIOCSWINSZ` (bubbletea gives up at a 0×0 window), auto-answer terminal queries (`ESC[6n`, OSC 10/11) so the first render isn't delayed, and assert on filesystem artifacts (key files, state JSON) rather than screen text alone. Confidence: 0.8
- Verify by measurement before declaring done: unit tests with fake `ssh`/`ssh-add` PATH shims, pty-driven end-to-end smoke in a sandboxed HOME, live GUI launch probes, and contrast math for UI color changes. Confidence: 0.75
- UX philosophy: guided wizard over dense consoles. Called the original multi-pane dashboard layout "very confusing and overwhelming"; wants apps that ask the user what they need and walk one decision per screen (pick task → settings form → result page highlighting the newly created item among a tree of keys → service pick → numbered step-by-step instructions → open browser at the right settings page → connection test → success page with next-action options). On failure, show basic troubleshooting that diagnoses the cause and walks the user through fixing it — don't just dump raw errors. Applies to both GUI and TUI alike. Confidence: 0.95
- On UX restructures prefers clean replacement over compatibility: chose "replace entirely" (wizard becomes the only interface) over keeping the old console layout as an advanced mode — simplest experience, least code to maintain. Confidence: 0.85
- Frontend parity for UX work: any flow or redesign must be implemented in BOTH the Fyne GUI and the Bubble Tea TUI ("similarly for tui too"), ideally derived from a single shared flow spec so the two stay in lockstep. Confidence: 0.85
- `gh` CLI is installed and authenticated (GitHub account `Leptons1618`); use it for GitHub operations — repo rename via REST API, `gh secret set` for Actions secrets, `gh api` for name-availability checks — instead of manual web-UI steps. Confidence: 0.85
- npm distribution preference: unscoped package name so installs stay simple (`npm i -g keysmith`); explicitly chose unscoped over a scoped `@user/` package. Confidence: 0.6
- Secrets hygiene: treat any credential pasted into chat as compromised — revoke it and supply the replacement out-of-band (local file / `gh secret set`), never through chat, logs, or the repo. User accepted this flow. Confidence: 0.6
- Renames are holistic: asked to rename the application AND repository together, and accepted a full-surface rename in one pass (Go module, cmd path, binaries, npm package/launcher, workflows, docs, GitHub repo). Confidence: 0.6
